/* ===== NOTES HERO ===== */
.notes-hero {
  background: linear-gradient(135deg, var(--primary), #5c0e0e);
  padding: 2.5rem 2rem 2rem;
  color: white;
  position: relative;
  overflow: hidden;
}
.breadcrumb {
  font-size: 0.85rem;
  color: rgba(245,230,200,0.7);
  margin-bottom: 0.5rem;
  font-style: italic;
}
.notes-hero h1 {
  font-family: var(--font-display);
  font-size: 2rem;
  color: var(--accent);
  margin-bottom: 0.3rem;
}
.notes-hero p { color: rgba(245,230,200,0.8); }

/* PRINT BTN */
.print-btn {
  background: rgba(255,255,255,0.15);
  border: none; color: #fff;
  padding: 0.3rem 0.9rem; border-radius: 20px;
  font-size: 0.85rem; cursor: pointer;
  font-family: var(--font-body);
}
.print-btn:hover { background: rgba(255,255,255,0.25); }

/* ===== NOTES CONTAINER ===== */
.notes-container {
  max-width: 900px;
  margin: 0 auto;
  padding: 2rem 1.5rem 3rem;
}

/* ===== CHAPTER PROGRESS ===== */
.chapter-progress {
  background: var(--card);
  border-radius: var(--radius);
  padding: 1.5rem;
  margin-bottom: 2rem;
  border: 1px solid var(--border);
  box-shadow: 0 4px 15px var(--shadow);
}
.chapter-progress h3 {
  font-family: var(--font-display);
  color: var(--primary);
  margin-bottom: 1rem;
}
[data-theme="dark"] .chapter-progress h3 { color: var(--accent); }
.chapters-list { display: flex; flex-direction: column; gap: 0.6rem; }
.chapter-check {
  display: flex; align-items: center; gap: 0.6rem;
  cursor: pointer; font-size: 0.98rem; color: var(--text);
}
.chapter-check input { accent-color: var(--primary); width: 16px; height: 16px; cursor: pointer; }

/* ===== TABS ===== */
.tabs {
  display: flex; gap: 0.5rem;
  margin-bottom: 1.5rem;
  flex-wrap: wrap;
}
.tab {
  padding: 0.6rem 1.2rem;
  border: 2px solid var(--border);
  border-radius: 25px;
  background: var(--card);
  color: var(--text-muted);
  font-family: var(--font-body);
  font-size: 0.95rem;
  cursor: pointer;
  transition: all 0.2s;
}
.tab.active, .tab:hover {
  background: var(--primary);
  color: var(--accent);
  border-color: var(--primary);
}

/* ===== TAB CONTENT ===== */
.tab-content { display: none; }
.tab-content.active { display: block; }

/* ===== NOTE CARD ===== */
.note-card {
  background: var(--card);
  border-radius: var(--radius);
  padding: 1.8rem;
  margin-bottom: 1.5rem;
  border: 1px solid var(--border);
  box-shadow: 0 4px 15px var(--shadow);
}
.note-card h2 {
  font-family: var(--font-display);
  font-size: 1.4rem;
  color: var(--primary);
  margin-bottom: 0.8rem;
  border-bottom: 2px solid var(--accent);
  padding-bottom: 0.5rem;
}
[data-theme="dark"] .note-card h2 { color: var(--accent); border-bottom-color: var(--primary); }
.note-card h3 {
  font-family: var(--font-display);
  font-size: 1.2rem;
  color: var(--primary);
  margin-bottom: 0.8rem;
}
[data-theme="dark"] .note-card h3 { color: var(--accent); }
.note-card p { color: var(--text); line-height: 1.8; font-size: 1rem; }
.key-points {
  background: rgba(139,26,26,0.05);
  border-left: 4px solid var(--accent);
  border-radius: 0 10px 10px 0;
  padding: 1rem 1.2rem;
  margin-top: 1rem;
}
.key-points h4 { color: var(--primary); margin-bottom: 0.5rem; font-size: 0.95rem; }
[data-theme="dark"] .key-points h4 { color: var(--accent); }
.key-points ul { padding-left: 1.2rem; }
.key-points li { color: var(--text); margin-bottom: 0.3rem; font-size: 0.95rem; }

/* ===== QUESTION LIST ===== */
.question-list { padding-left: 1.4rem; }
.question-list li {
  color: var(--text);
  margin-bottom: 0.9rem;
  line-height: 1.7;
  font-size: 1rem;
}

/* ===== PDF ===== */
.upload-box {
  border: 2px dashed var(--border);
  border-radius: var(--radius);
  padding: 2.5rem;
  text-align: center;
  margin-bottom: 1.5rem;
  background: var(--card);
}
.upload-icon { font-size: 2.5rem; margin-bottom: 0.7rem; }
.upload-box h3 { font-family: var(--font-display); color: var(--primary); margin-bottom: 0.4rem; }
[data-theme="dark"] .upload-box h3 { color: var(--accent); }
.upload-box p { color: var(--text-muted); font-size: 0.9rem; margin-bottom: 1rem; }
.upload-btn {
  background: var(--primary); color: white;
  border: none; padding: 0.6rem 1.5rem;
  border-radius: 25px; cursor: pointer;
  font-family: var(--font-body); font-size: 0.95rem;
  transition: background 0.2s;
}
.upload-btn:hover { background: var(--primary-light); }

.pdf-links { background: var(--card); border-radius: var(--radius); padding: 1.5rem; border: 1px solid var(--border); }
.pdf-links h3 { font-family: var(--font-display); color: var(--primary); margin-bottom: 1rem; }
[data-theme="dark"] .pdf-links h3 { color: var(--accent); }
.pdf-item {
  display: flex; justify-content: space-between; align-items: center;
  padding: 0.7rem 0; border-bottom: 1px solid var(--border);
  font-size: 0.95rem; color: var(--text);
}
.pdf-item:last-child { border-bottom: none; }
.pdf-dl-btn {
  background: var(--accent2); color: white;
  padding: 0.3rem 0.9rem; border-radius: 20px;
  font-size: 0.82rem; font-weight: 600;
  transition: opacity 0.2s;
}
.pdf-dl-btn:hover { opacity: 0.8; }

/* ===== VIDEO ===== */
.video-list { background: var(--card); border-radius: var(--radius); padding: 1.5rem; border: 1px solid var(--border); }
.video-list h3 { font-family: var(--font-display); color: var(--primary); margin-bottom: 1rem; }
[data-theme="dark"] .video-list h3 { color: var(--accent); }
.video-item {
  display: flex; gap: 1.2rem; align-items: flex-start;
  padding: 1rem 0; border-bottom: 1px solid var(--border);
}
.video-item:last-child { border-bottom: none; }
.video-thumb {
  width: 56px; height: 40px; flex-shrink: 0;
  background: var(--primary);
  border-radius: 8px;
  display: flex; align-items: center; justify-content: center;
  color: var(--accent); font-size: 1.3rem;
  cursor: pointer;
}
.video-item strong { display: block; color: var(--text); margin-bottom: 0.2rem; }
.video-item p { color: var(--text-muted); font-size: 0.85rem; margin-bottom: 0.4rem; }
.url-input {
  width: 100%; border: 1px solid var(--border);
  border-radius: 8px; padding: 0.3rem 0.7rem;
  background: var(--bg); color: var(--text);
  font-family: var(--font-body); font-size: 0.85rem;
}
.url-input:focus { outline: none; border-color: var(--primary); }

/* ===== PRINT ===== */
@media print {
  .navbar, .tabs, .chapter-progress, .print-btn, .upload-box, .video-list { display: none; }
  .tab-content { display: block !important; }
  .note-card { box-shadow: none; border: 1px solid #ccc; }
}

/* ===== RESPONSIVE ===== */
@media (max-width: 600px) {
  .notes-container { padding: 1.5rem 1rem; }
  .tabs { gap: 0.4rem; }
  .tab { padding: 0.5rem 0.9rem; font-size: 0.85rem; }
}
